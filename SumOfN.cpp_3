int main() {
    int num;
    int sum = 0; 

    cout << "enter number to calc the sum : " << endl;
    cin >> num;

    for (int i = 1; i <= num; i++) {
        sum = sum + i;
    }

    cout << "the sum is :" << sum << endl;
    return 0;
}
